World implements Sharable {
	println(char[] world) {
	}

	main () {
		generate-env
		pick starting value
		pick destination
		createPlayer(starting, dest)
		while (player.notReached()) {
			println(player.step());
		}
	}
}
